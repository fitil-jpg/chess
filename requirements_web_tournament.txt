# Web-based Chess Tournament System Requirements

# Core web framework
flask>=2.0.0
flask-socketio>=5.0.0

# Chess library
python-chess>=1.9.0

# Existing chess AI dependencies (if not already installed)
# These should already be available in your environment
# chess-ai modules
# evaluation module
# pattern detection modules

# Optional: For better performance
eventlet>=0.33.0